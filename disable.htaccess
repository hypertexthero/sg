# Using nginx for deployment, so this is not used anymore.
# Leaving here temporarily in case I need to go back to Apache.
# <IfModule mod_rewrite.c>
#     RewriteEngine On
#     RewriteCond %{THE_REQUEST} \.html
#     RewriteRule ^(.*)\.html$ /$1 [R=301,L]
# </IfModule>